from setuptools import setup, find_packages

setup(
    name='gymnasium_trial',
    version='0.0.1',
    author='Jason Chen',
    packages=find_packages(),
    install_requires=[
        "gymnasium",
    ],
)
